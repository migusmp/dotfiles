# ======================
# ANIMATIONS (smooth)
# ======================

animations {
  enabled = true

  bezier = easeOut, 0.16, 1, 0.3, 1
  bezier = easeInOut, 0.4, 0, 0.2, 1

  animation = windows, 1, 6, easeOut, slide
  animation = windowsOut, 1, 6, easeInOut, slide

# animation = windows, 1, 7, easeOut, slide
# animation = windowsOut, 1, 6, easeInOut, slide

  # animation = windows, 1, 5, easeOut, slide
  # animation = windowsOut, 1, 4, easeInOut, slide

  animation = border, 1, 5, easeOut
  animation = fade, 1, 5, easeInOut
  animation = workspaces, 1, 6, easeOut, slide
}

# animations {
#   enabled = true
#
#   # Curvas modernas (suaves y con punch)
#   bezier = easeOut, 0.16, 1, 0.3, 1
#   bezier = easeInOut, 0.4, 0, 0.2, 1
#   bezier = smoothOut, 0.2, 1, 0.2, 1
#   bezier = smoothInOut, 0.65, 0, 0.35, 1
#
#   # Ventanas: entrada con slide “premium”, salida un pelín más rápida
#   animation = windows, 1, 7, smoothOut, slide
#   animation = windowsOut, 1, 6, smoothInOut, slide
#
#   # Movimiento al reordenar / mover: se nota MUCHO en dwindle
#   animation = windowsMove, 1, 6, smoothInOut
#
#   # Fade general (para menús/capas y cambios suaves)
#   animation = fade, 1, 6, easeInOut
#
#   # Bordes: sutil pero elegante
#   animation = border, 1, 5, smoothOut
#
#   # Workspaces: rápido y limpio
#   animation = workspaces, 1, 7, smoothOut, slide
# }
#
#
